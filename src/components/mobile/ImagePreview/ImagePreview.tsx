type ImagePreviewProps = {
    alt: string;
    current: number;
    images:  string[];
    getContainer: string;
}

const ImagePreview: React.FC<ImagePreviewProps> = () => {

    return (
        <div>
            Image
        </div>
    )
}

export default ImagePreview
